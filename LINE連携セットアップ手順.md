# ベジログ × LINE連携 セットアップ手順

## 全体の仕組み

このLINE連携は、次の3つのパーツで構成されています。

- **ベジログアプリ（vegelog.html）**：野菜の記録や血管年齢の計測をブラウザ上で行うアプリ本体。
- **LINE公式アカウント（Messaging API）**：学生がLINEで友だち追加し、通知を受け取る窓口。
- **Google Apps Script（GAS）**：LINEからのメッセージを受け取ったり、決まった時刻に自動でリマインダーを送信したりする「裏方のサーバー」役。データはGoogleスプレッドシートに保存されます。

流れとしては、学生がベジログで記録するたびに、そのデータがGASへ送られてスプレッドシートに蓄積されます。GASは毎日決まった時刻に、その日まだ記録していない学生や、計測から1〜2週間経った学生を判定し、LINEでメッセージを送ります。

セットアップは全部で10ステップです（STEP 9のアプリ公開は任意）。所要時間はおおよそ20〜30分です。

---

## STEP 1: LINE公式アカウントを作成する

1. [LINE Official Account Manager](https://manager.line.biz/) にアクセスし、LINEアカウントでログインします。
2. 「アカウントを作成」から、ベジログ用の公式アカウント（例: 「ベジログ通知」）を作成します。業種などの質問は適当なもので構いません。
3. 作成が完了したら、そのままSTEP 2に進みます。

## STEP 2: Messaging APIを有効化してトークンを発行する

1. [LINE Developers コンソール](https://developers.line.biz/console/) にログインします（STEP 1と同じLINEアカウントでログインすると、作成した公式アカウントがプロバイダー内に表示されます）。
2. 対象のチャネル（STEP 1で作った公式アカウント）を開き、「Messaging API設定」タブを開きます。
3. 「チャネルアクセストークン（長期）」の欄で「発行」を押し、表示された文字列をコピーして控えておきます（STEP 5で使います）。
4. 同じ画面で「応答メッセージ」をオフ、「Webhookの利用」をオンにしておきます（Webhook URLはSTEP 6で設定します）。

## STEP 3: Googleスプレッドシート＋Apps Scriptを準備する

1. [Google Sheets](https://sheets.new) で新しいスプレッドシートを作成し、わかりやすい名前（例: 「ベジログ通知DB」）を付けます。
2. メニューの「拡張機能」→「Apps Script」を開きます。
3. エディタに最初から入っている `function myFunction() {}` などのコードを全て削除し、代わりに `vegelog-line-bot.gs.txt` の中身を全てコピー＆貼り付けします。
4. 左上のプロジェクト名を「ベジログ通知ボット」などに変更し、保存します（フロッピーのアイコン、またはCtrl+S）。

## STEP 4: スクリプトプロパティにアクセストークンを設定する

1. Apps Scriptエディタの左メニューにある歯車アイコン「プロジェクトの設定」を開きます。
2. 一番下の「スクリプト プロパティ」で「スクリプト プロパティを追加」を押します。
3. プロパティ名に `CHANNEL_ACCESS_TOKEN`、値にSTEP 2でコピーしたチャネルアクセストークンを貼り付けて保存します。

## STEP 5: ウェブアプリとしてデプロイする

1. エディタ右上の「デプロイ」→「新しいデプロイ」を押します。
2. 種類の選択で「ウェブアプリ」を選びます。
3. 「次のユーザーとして実行」は「自分」、「アクセスできるユーザー」は「全員」を選択します（LINEやベジログアプリからアクセスできるようにするため）。
4. 「デプロイ」を押すと、`https://script.google.com/macros/s/.../exec` という形式のURLが発行されます。このURLを控えておいてください（STEP 6・8で使います）。
5. 初回は権限の承認を求められるので、画面の指示に従って自分のGoogleアカウントで承認してください。

> コードを後から修正した場合は、「新しいデプロイ」ではなく「デプロイを管理」→ 鉛筆アイコンから「新しいバージョン」を選んで更新してください。新しいデプロイを作るとURLが変わってしまいます。

## STEP 6: LINE DevelopersにWebhook URLを設定する

1. LINE Developersコンソールの「Messaging API設定」タブに戻ります。
2. 「Webhook URL」にSTEP 5で発行されたURLを貼り付け、「更新」を押します。
3. 「検証」ボタンを押します。

> **注意:** GAS（Google Apps Script）製のWebhookでは、この「検証」ボタンが `ボットサーバーから200以外のHTTPステータスコードが返されました。(302 Found)` というエラーを出すことがよくあります。これはGASのWebアプリURLが仕様上まず302リダイレクトを返すためで、検証ボタンがそのリダイレクトを追いかけないことによる見せかけのエラーです。実際のLINEメッセージ（友だち追加やトーク送信）は正常にGASへ届くことがほとんどなので、この検証エラーは無視してSTEP 9の実機テストに進んでください。実機テストでも反応がない場合のみ、トラブルシューティングの項目を確認してください。

## STEP 7: 通知トリガーを設定する

1. Apps Scriptエディタに戻り、上部の関数選択プルダウンから `setupTriggers` を選びます。
2. 実行ボタン（▷）を押します。初回は権限承認が求められる場合があります。
3. 左メニューの時計アイコン「トリガー」を開き、`notifyForgotten`（毎日21時）、`notifyCanteen`（毎日11時）、`notifyRecall`（毎日9時）の3つが登録されていることを確認します。

これで、毎日自動でリマインダーが送信される状態になりました。時刻や文面を変えたい場合は、`vegelog-line-bot.gs.txt` 内の該当関数を書き換えてから「新しいバージョン」でデプロイし直してください。

## STEP 8: ベジログアプリにGAS WebアプリURLを設定する

1. ベジログアプリを開き、「設定」タブの「LINE連携」欄に、STEP 5で控えたGAS WebアプリURLを貼り付けます。
2. 同じ画面に表示される「被験者ID」を確認します（アプリの初回登録で入力したIDです）。

### （任意）通知メッセージにベジログへのリンクを付ける

vegelog.htmlをインターネット上に公開すると（STEP 9参照）、そのURLを通知メッセージの末尾に自動で付けることができます。

1. 公開したvegelog.htmlのURLを控えます。
2. Apps Scriptエディタの「プロジェクトの設定」→「スクリプト プロパティ」で、`VEGELOG_URL` という名前でそのURLを追加します。
3. 以降、記録忘れリマインダーなどのメッセージ末尾に「ベジログを開く: （URL）」が自動で付き、LINE上でタップするとそのままアプリが開くようになります。

## STEP 9: vegelogアプリをインターネットに公開する（任意）

これまでの手順では、ベジログアプリ（vegelog.html）はお使いのパソコンに保存されたファイルとして動作しています。自分のパソコンでだけ使うなら公開は不要ですが、スマートフォンから開きたい場合や、他の学生にも配ってもらう場合は、どこかにアップロードしてURLを発行する必要があります。

vegelog.htmlはサーバー側の処理を必要としない「静的な1枚のHTMLファイル」なので、以下のような無料サービスにそのままアップロードするだけで公開できます。

- **Netlify Drop**（最も手軽・お試し向け）：<https://app.netlify.com/drop> を開き、vegelog.htmlをブラウザにドラッグ＆ドロップするだけでURLが発行されます。アカウント登録なしでも使えますが、その場合は数十分〜1時間程度で公開が切れる点に注意してください。継続して使うにはアカウント登録（無料）をしてください。
- **GitHub Pages**（無料・恒久的に公開したい場合向け）：GitHubアカウントを作成し、新しいリポジトリにvegelog.htmlをアップロードして、リポジトリの Settings → Pages で公開を有効にします。`https://ユーザー名.github.io/リポジトリ名/vegelog.html` のようなURLが発行されます。
- **Cloudflare Pages**：Cloudflareのダッシュボードから「Direct Upload」を選び、vegelog.htmlが入ったフォルダをドラッグ＆ドロップして公開します。

> **注意（データの扱いについて）:** ベジログの記録データ（食事記録・血管年齢の値・APIキー・GAS URLなど）は、それぞれの利用者が開いているブラウザの中だけに保存されます（サーバーには送られません）。そのため、同じURLを複数人に配って使ってもらっても、各自の記録が他の人に見えることはありません。ただし、Anthropic APIキーを使う「写真AI判定」機能を使う場合は、利用者一人ひとりが自分でAPIキーを取得して設定タブに入力する必要があります（キーを共有すると、その人にも使われてしまうため推奨しません）。

## STEP 10: 動作確認

1. スマートフォンのLINEアプリで、STEP 1の公式アカウントを友だち追加します（LINE Developersの「Messaging API設定」タブにQRコードがあります）。
2. 追加すると挨拶メッセージが届くので、その返信として自分の被験者ID（例: `S001`）を送信します。
3. 「被験者IDで連携しました」という返信が届けば連携完了です。
4. ベジログアプリで食事を1件記録し、Googleスプレッドシートの「Logs」シートに行が追加されていることを確認します。
5. リマインダーを今すぐ試したい場合は、Apps Scriptエディタで `notifyForgotten` などの関数を選んで手動実行すると、その場でLINEに届きます（本番では時刻が来ると自動実行されます）。

---

## トラブルシューティング

**「検証」ボタンで `302 Found` エラーが出る場合**
GAS製Webhookでは既知の現象で、実際のメッセージ送受信には影響しないことがほとんどです。無視してSTEP 9の実機テスト（友だち追加してトークを送る）で本当に動くか確認してください。実機でも反応がなければ、下記の項目を確認してください。

**LINEにメッセージが届かない場合**
チャネルアクセストークンがスクリプトプロパティに正しく設定されているか、デプロイの「アクセスできるユーザー」が「全員」になっているかを確認してください。Apps Scriptエディタの「実行数」（左メニューの再生ボタンのようなアイコン）から、エラーの詳細ログを確認できます。

**「被験者IDで連携しました」の返信が来ない場合**
LINE Developers側で「応答メッセージ」がオンのままだと、Webhookより先にLINE標準の自動応答が動いてしまうことがあります。「応答メッセージ」はオフ、「Webhookの利用」はオンになっているか再確認してください。

**ベジログの記録がスプレッドシートに反映されない場合**
ベジログの「設定」タブに貼り付けたURLの末尾が `/exec` になっているか確認してください（`/dev` ではなく本番URLの `/exec` を使います）。また、コードを更新した際に「新しいデプロイ」を選んでしまうとURLが変わるので、その場合はベジログ側のURLも更新してください。

---

## 料金・プライバシーに関する注意

Google Apps Scriptと連携用のGoogleスプレッドシートは無料で利用できます（個人のGoogleアカウントの範囲内）。LINE公式アカウントのMessaging APIも、月1,000通までの無料プランの範囲であれば費用はかかりません（人数や送信頻度が多い場合は有料プランの検討が必要です）。

このスプレッドシートには被験者IDと食事記録・血管年齢のデータが蓄積されます。研究データを扱う場合は、スプレッドシートの共有設定を必要な人だけに限定し、アクセス権限の管理を徹底してください。
